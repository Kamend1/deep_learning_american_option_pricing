[pytest]
testpaths = tests
addopts = -ra -m "not integration and not slow"
markers =
    integration: miniature end-to-end tests across multiple project modules
    slow: longer Monte Carlo, convergence, or neural-training smoke tests
